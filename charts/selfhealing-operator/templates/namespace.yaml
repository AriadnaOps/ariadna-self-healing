{{- if .Values.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "selfhealing-operator.namespace" . }}
  labels:
    {{- include "selfhealing-operator.labels" . | nindent 4 }}
{{- end }}
