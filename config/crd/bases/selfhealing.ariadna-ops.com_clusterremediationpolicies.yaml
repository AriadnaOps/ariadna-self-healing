---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: clusterremediationpolicies.selfhealing.ariadna-ops.com
spec:
  group: selfhealing.ariadna-ops.com
  names:
    kind: ClusterRemediationPolicy
    listKind: ClusterRemediationPolicyList
    plural: clusterremediationpolicies
    shortNames:
    - crp
    - rempol
    singular: clusterremediationpolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.active
      name: Active
      type: boolean
    - jsonPath: .spec.globalConfig.dryRun
      name: Dry-Run
      type: boolean
    - jsonPath: .status.enabledScenarios
      name: Scenarios
      type: integer
    - jsonPath: .status.matchedResources
      name: Resources
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterRemediationPolicy is the Schema for the clusterremediationpolicies
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterRemediationPolicySpec defines the desired state of
              ClusterRemediationPolicy
            properties:
              globalConfig:
                description: GlobalConfig applies global settings
                properties:
                  defaultCooldown:
                    description: DefaultCooldown is the default cooldown for all scenarios
                    type: string
                  defaultMaxRetries:
                    description: DefaultMaxRetries is the default max retries for
                      all scenarios
                    type: integer
                  dryRun:
                    default: false
                    description: DryRun enables dry-run mode for this policy
                    type: boolean
                  escalation:
                    description: Escalation configures global escalation settings
                    properties:
                      defaultChannels:
                        description: DefaultChannels are the default notification
                          channels for escalation
                        items:
                          type: string
                        type: array
                      enabled:
                        default: true
                        description: Enabled enables escalation
                        type: boolean
                    type: object
                  notificationChannels:
                    description: NotificationChannels defines available notification
                      channels
                    items:
                      description: NotificationChannel defines a notification channel
                      properties:
                        name:
                          description: Name is the channel name (referenced in escalation)
                          type: string
                        secretRef:
                          description: SecretRef references a secret containing credentials
                          properties:
                            key:
                              description: Key is the key in the secret data
                              type: string
                            name:
                              description: Name is the secret name
                              type: string
                            namespace:
                              description: Namespace is the secret namespace (defaults
                                to operator namespace)
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        type:
                          description: Type is the channel type
                          enum:
                          - webhook
                          - slack
                          - pagerduty
                          type: string
                        url:
                          description: URL is the endpoint URL
                          type: string
                      required:
                      - name
                      - type
                      - url
                      type: object
                    type: array
                  paused:
                    default: false
                    description: Paused temporarily disables this policy
                    type: boolean
                type: object
              scenarioConfigs:
                description: ScenarioConfigs configures individual scenarios
                items:
                  description: ScenarioConfig configures an individual scenario
                  properties:
                    additionalLabels:
                      additionalProperties:
                        type: string
                      description: AdditionalLabels adds labels for metrics/logging
                      type: object
                    cooldown:
                      description: Cooldown overrides the scenario's cooldown
                      type: string
                    enabled:
                      description: Enabled overrides the scenario's enabled setting
                      type: boolean
                    maxRetries:
                      description: MaxRetries overrides the scenario's max retries
                      type: integer
                    name:
                      description: Name is the scenario name (from ScenarioLibrary)
                      type: string
                    parameters:
                      additionalProperties:
                        type: string
                      description: Parameters overrides scenario parameters
                      type: object
                  required:
                  - name
                  type: object
                type: array
              scenarioLibraryRef:
                description: ScenarioLibraryRef references the ScenarioLibrary to
                  use
                properties:
                  name:
                    description: Name is the name of the ScenarioLibrary
                    type: string
                required:
                - name
                type: object
              targetSelector:
                description: TargetSelector selects which resources this policy applies
                  to
                properties:
                  excludedNamespaces:
                    description: ExcludedNamespaces to exclude from monitoring
                    items:
                      type: string
                    type: array
                  labelSelector:
                    description: LabelSelector filters resources by labels
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  namespaces:
                    description: Namespaces to include (empty = all)
                    items:
                      type: string
                    type: array
                  resourceTypes:
                    description: ResourceTypes specifies which resource types to monitor
                    items:
                      type: string
                    type: array
                type: object
            required:
            - scenarioLibraryRef
            - targetSelector
            type: object
          status:
            description: ClusterRemediationPolicyStatus defines the observed state
              of ClusterRemediationPolicy
            properties:
              active:
                description: Active indicates if the policy is currently active
                type: boolean
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              detectionStats:
                description: DetectionStats contains detection statistics
                properties:
                  byScenario:
                    additionalProperties:
                      format: int64
                      type: integer
                    description: ByScenario counts detections by scenario
                    type: object
                  last24Hours:
                    description: Last24Hours is detections in the last 24 hours
                    format: int64
                    type: integer
                  totalDetections:
                    description: TotalDetections is the total number of detections
                    format: int64
                    type: integer
                type: object
              enabledScenarios:
                description: EnabledScenarios is the count of enabled scenarios
                type: integer
              lastEvaluated:
                description: LastEvaluated is when the policy was last evaluated
                format: date-time
                type: string
              matchedResources:
                description: MatchedResources is the count of resources matching the
                  selector
                type: integer
              remediationStats:
                description: RemediationStats contains remediation statistics
                properties:
                  escalationCount:
                    description: EscalationCount is the number of escalations
                    format: int64
                    type: integer
                  failureCount:
                    description: FailureCount is the number of failed remediations
                    format: int64
                    type: integer
                  last24Hours:
                    description: Last24Hours is remediations in the last 24 hours
                    format: int64
                    type: integer
                  skippedCount:
                    description: SkippedCount is the number of skipped remediations
                      (cooldown)
                    format: int64
                    type: integer
                  successCount:
                    description: SuccessCount is the number of successful remediations
                    format: int64
                    type: integer
                  totalRemediations:
                    description: TotalRemediations is the total number of remediations
                    format: int64
                    type: integer
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
